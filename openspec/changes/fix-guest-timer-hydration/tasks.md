# Tasks: Fix guest timer hydration mismatch (Issue #44)

Strict TDD. Sequence: RED test → GREEN implementation, per task. All new/modified
files live under `src/ui/`; no route, page, or public-contract change.

## 1. Shared `useHydrated` hook (sequential, blocks 2 and 3)

- [x] **1.1** Write failing tests in `src/ui/hooks/__tests__/use-hydrated.test.ts`
      (`// @vitest-environment jsdom`):
  - `should return true on a client render` — `renderHook(() => useHydrated())`, expect `true`.
  - `should return false in the server snapshot` — `renderToStaticMarkup(createElement(Probe))`
    where `Probe` renders `useHydrated()` into text; expect `"false"`. This is the
    load-bearing assertion — the client-only test alone would pass against a broken
    `getServerSnapshot`. Keep the file `.ts` (no JSX), build the probe via `createElement`.
  - Satisfies: `ui-structure` — "one shared hook, not reimplemented per consumer".
- [x] **1.2** Implement `src/ui/hooks/use-hydrated.ts` per design.md's interface
      (`"use client"`, `useSyncExternalStore`, no-op `subscribe`, `getSnapshot -> true`,
      `getServerSnapshot -> false`, exported `useHydrated`). Make 1.1 pass.
  - **Contingency**: if `renderToStaticMarkup` from `react-dom/server` is not resolvable
    under the jsdom Vitest environment, move only the SSR test into its own file without
    the `@vitest-environment jsdom` docblock (default `node` environment) and re-run.
    Do not silently drop or weaken the SSR assertion.

## 2. Gate `useGuestTimerForm` (parallel with 3, depends on 1)

- [x] **2.1** Confirm `src/ui/components/guest-timer-form/__tests__/guest-timer-form.hook.test.ts`
      still passes unmodified after 2.2 (regression bar — do not edit this file; treat any
      need to edit it as a red flag to report, not silently accommodate).
- [x] **2.2** In `guest-timer-form.hook.ts`: add `const isHydrated = useHydrated();`,
      prepend `if (!isHydrated) return;` as the effect's first statement (before
      `let cancelled = false;`), add `isHydrated` to the dependency array
      (`[isHydrated, localAdapter]`). No signature, return-type, or `defaultLocalAdapter`
      change.
  - Satisfies: `ui-structure` (gated setter), `guest-timer-start-flow` scenario
    "`/guest-timer` reloads clean with a saved configuration".

## 3. Gate `useGuestTimerActive` (parallel with 2, depends on 1)

- [x] **3.1** Confirm `src/ui/components/guest-timer-active/__tests__/guest-timer-active.hook.test.ts`
      still passes unmodified after 3.2 (same regression bar as 2.1).
- [x] **3.2** In `guest-timer-active.hook.ts`: add `const isHydrated = useHydrated();`,
      prepend `if (!isHydrated) return;` as the effect's first statement, add `isHydrated`
      to the dependency array (`[isHydrated, localAdapter, router]`). No signature,
      return-type, or `defaultLocalAdapter` change.
  - Satisfies: `ui-structure` (gated setter/redirect), `guest-timer-start-flow`
    scenarios "`/guest-timer-active` reloads clean with a saved configuration" and
    "Guard redirect on `/guest-timer-active` still fires".

## 4. Verification (sequential, after 1-3)

- [x] **4.1** `npm run lint`, `npx tsc --noEmit`, `npm run test` all green.
- [x] **4.2** `Grep` the full diff for `suppressHydrationWarning` — must appear nowhere.
- [ ] **4.3** Manual browser check (the actual Issue #44 acceptance bar, never
      reproduced during planning): `npm run build && npm run start`, populate
      `localStorage` with a guest config, reload `/guest-timer` and `/guest-timer-active`,
      confirm a clean console on both.

## Review Workload Forecast

- Estimated changed lines: ~90 (per proposal.md forecast: 1 new hook ~12 lines, 1 new
  test file ~30-40 lines, 2 one-line effect gates + dependency-array edits).
- Chained PRs recommended: No
- 400-line budget risk: Low
- Decision needed before apply: No
