# Active changes

Each active change lives in `changes/{change-name}/` with:
`state.yaml`, optional `exploration.md`, `proposal.md`, `specs/`,
`design.md`, `tasks.md`, `verify-report.md`.

Completed changes move to `changes/archive/YYYY-MM-DD-{change-name}/`.