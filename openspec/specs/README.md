# Source of truth — main specs

This directory holds the merged, authoritative specs grouped by domain.
Each domain lives at `specs/{domain}/spec.md` and is updated by `sdd-archive`
after a change completes. It starts empty until the first change is archived.